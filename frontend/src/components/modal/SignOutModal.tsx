'use client';

import React from 'react';

import styled from 'styled-components';

const SignOutModal = () => {
  return <SignOutModalBlock></SignOutModalBlock>;
};

export default SignOutModal;

const SignOutModalBlock = styled.div``;
